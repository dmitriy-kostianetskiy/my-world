import { CountrySelectorContainerComponent } from './country-selector-container.component'

describe('CountrySelectorContainerComponent', () => {
  it('should mount', () => {
    cy.mount(CountrySelectorContainerComponent)
  })
})